import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: "com.yourcompany.trippack",
  appName: "TripPack",
  webDir: ".output/public",
  bundledWebRuntime: false
};

export default config;
